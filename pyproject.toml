[project]
name = "hydra-auth"
version = "0.1.0"
requires-python = ">=3.14,<4"
dependencies = [
    "hydra",
]

[tool.uv.workspace]
members = [
    "backend",
]

[tool.uv.sources]
hydra = { workspace = true }

[dependency-groups]
dev = [
    "black>=26.1.0",
]
